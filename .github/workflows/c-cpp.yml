name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make build folder
      run: mkdir build
    - name: show
      run: cd build && ls -la
    - name: cmake
      run: cd build && cmake ..
    - name: make
      run: cd build && make
      
